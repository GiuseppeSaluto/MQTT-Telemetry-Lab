[package]
name = "ingestion"
version = "0.1.0"
edition = "2021"

[dependencies]
# rumqttc = "0.24"        # MQTT client
# tokio = { version = "1", features = ["full"] }
# sqlx = { version = "0.7", features = ["runtime-tokio", "postgres"] }  # or tokio-postgres
# serde = { version = "1", features = ["derive"] }
# serde_json = "1"
